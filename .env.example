# El MVP v0.1 no requiere env vars — todo corre cliente-local.
# Este fichero queda como plantilla para futuras versiones (v0.4+ podría
# introducir post-procesado de la crónica con un LLM).
#
# Ejemplo de cómo se declararían cuando toque:
# GEMINI_API_KEY="tu_api_key"
